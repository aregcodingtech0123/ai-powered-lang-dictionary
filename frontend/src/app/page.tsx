import { DictionarySearch } from "@/components/DictionarySearch";

export default function Home() {
  return (
    <main className="bg-[var(--background)] font-[family-name:var(--font-geist-sans)]">
      <DictionarySearch />
    </main>
  );
}
